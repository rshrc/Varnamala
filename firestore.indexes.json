{
  "indexes": [
    {
      "// note": "Language-scoped league boards. getLeagueLeaderboard filters users by the language they are learning and takes the top scorers of that language, which Firestore cannot serve without this composite index.",
      "collectionGroup": "users",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "languages", "arrayConfig": "CONTAINS" },
        { "fieldPath": "score", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
